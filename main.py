import argparse
from pathlib import Path
parser = argparse.

print(args)

sourse = Path(arg.get("sourse"))
output = Path(arg.get("output"))

folders = []


def new_folder(path: Path);
    pass


def copy_file(path: Path);
    pass

if __name__=="main":
    logging.basicConfig(level=loggin.INFO, format="%(threadName)s %(message)s")
    folders.append(sourse)
    new_folder(sourse)